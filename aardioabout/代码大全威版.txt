官网
http://bbs.aardio.com/
--------------------------------------------------------------------------------
控制台打印
import console;
	console.open();
	console.print("aaaaaaaaaaaaaaaaaaaa");
	console.pause(true);
--------------------------------------------------------------------------------
文件选择
import fsys.dlg;

var dir=fsys.dlg.open("*.txt");
--------------------------------------------------------------------------------
按行读文件

var myDir="d:\1.txt";
	for line in io.lines( myDir) { 
      win.msgbox(line);
	}
--------------------------------------------------------------------------------
遍历目录下所有文件
import fsys;
var myPath1="d:/11";
    fsys.enum( myPath1, "*.*",
    function(dir,file,fullpath,findData){ 
       判断文件名中是否有某字符串
       if string.find(file,文件名中包含的字符串)!=null{
       //删除文件
       fsys.delete(fullpath);
       count=count+1;
       }
    }
   );
--------------------------------------------------------------------------------
取edit中的值
mainForm.edit1.text
--------------------------------------------------------------------------------
弹出提示框窗口
win.msgbox("内容","标题");
--------------------------------------------------------------------------------
获取网页中元素并赋值
var ea1=wb.getEle("msisdn");
ea1.value="18888888888";
//自动提交表单仅限于有form的
ea1.form.submit();
--------------------------------------------------------------------------------
将浏览器刷入控件
import web.form;
import web.form.util;

var wb = web.form( mainForm.edit );
wb.external = mainForm.edit;

//允许跨域操作
web.form.util.crossDomain(true);
//加载指定网址到浏览器
wb.go("http://www.baidu.com");
wb.wait("");
--------------------------------------------------------------------------------
网页自动点击
import key;
var btsub=f1.getEle("button1");
//元素得焦点
	btsub.focus();
	//元素单击
	btsub.click();
	//触发onclick事件
	btsub.fireEvent("onclick");
	//模拟回车键
	//key.press(key.VK.ENTER);
	
--------------------------------------------------------------------------------
转换iframe为webform用来操作iframe中元素
var f1=wb.getWebForm("frame");
--------------------------------------------------------------------------------
延时处理1000为1秒
win.delay(1000);
--------------------------------------------------------------------------------
for循环
	console.open();
	
	for(i=0;10;1){
	win.delay(1000);
	console.print(i);
	}
	console.pause(true);
--------------------------------------------------------------------------------
获取html文档内容
var doc = wb.getDoc(0)
--------------------------------------------------------------------------------
不显示网页JS弹出的【确定】窗口
wb.showMsg=function( 提示信息, 对话框标题 ){
    return false;/*返回false则不显示对话框*/
}
--------------------------------------------------------------------------------
获取网页内容
wb.body.innerHTML
--------------------------------------------------------------------------------
查找子串
string.find(源字符串,要找的字符串)
--------------------------------------------------------------------------------
写文件。包括换行写文件
string.save("/失败201707071745.txt", "ffaaaaf",true)
string.save("/失败201707071745.txt", '\r\n',true)
--------------------------------------------------------------------------------
自定义函数
function ts1(wb){
    wb.go(mainForm.edit2.text);	
}
--------------------------------------------------------------------------------
获取字符串长度
var count=string.len(mainForm.edit1.text);
--------------------------------------------------------------------------------
一个字符一个字符显示
	var count=string.len(mainForm.edit1.text);
	mainForm.static.text=count;
	for(i=1;count;1){
	win.delay(1000);
	mainForm.static.text=string.sub(mainForm.edit1.text,i,i);
	}
--------------------------------------------------------------------------------
按向前删除键
    key.press(key.VK.BACK);
--------------------------------------------------------------------------------
清空请求缓存
inet.clearCache();
--------------------------------------------------------------------------------
执行javascript脚本
//1获取文档
var myDoc=wb.getDoc();
//2需注意使用wb.doScript方法时，JS函数要用双引号""括住
myDoc.doScript("test('1')");
--------------------------------------------------------------------------------
执行javascript脚本
//1获取文档
var myDoc=wb.getDoc();
//2使用Script函数
myDoc.Script.test('1');
--------------------------------------------------------------------------------
获取当前时间
import time;
time.now();
--------------------------------------------------------------------------------
获取当前路径
fsys.getCurDir()
--------------------------------------------------------------------------------
截取字符串
string.sub(原始字符串,起始坐标,结束坐标);
--------------------------------------------------------------------------------
文件重命名
 fsys.rename(旧文件名,新文件名);
--------------------------------------------------------------------------------